{
  "name": "@eiflow/bot-cyrene",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Ei Flow — Cyrene (AI — assistant (Mistral) and Cyrene persona (gpt-oss))",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "deploy:commands": "tsx scripts/deploy-commands.ts"
  },
  "dependencies": {
    "@eiflow/shared": "*",
    "discord.js": "^14.15.3"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "tsx": "^4.19.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
