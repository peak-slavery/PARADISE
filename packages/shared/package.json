{
  "name": "@eiflow/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Ei Flow shared runtime — embeds, server-lock, threat scoring, rate limiting, dual-DB clients, logging, Sentry, health, lazy command loading",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@sentry/node": "^10.72.0",
    "@supabase/supabase-js": "^2.45.4",
    "@upstash/redis": "^1.34.3",
    "discord.js": "^14.15.3",
    "mongodb": "^6.9.0",
    "pino": "^9.4.0",
    "vitest": "^3.2.7",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.5"
  }
}
