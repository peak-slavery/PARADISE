version: 2
updates:
  - package-ecosystem: npm
    directory: '/'
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      # One PR for the whole workspace instead of 10.
      all-dependencies:
        patterns: ['*']
    labels: ['dependencies']
